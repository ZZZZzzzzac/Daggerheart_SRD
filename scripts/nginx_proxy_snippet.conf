# 在 nginx server 块中添加以下 location，放在现有 /SRD/ location 之前：
#
#   location /SRD/api/ {
#       proxy_pass http://127.0.0.1:5000/api/;
#       proxy_set_header Host $host;
#       proxy_set_header X-Real-IP $remote_addr;
#   }
#
# 完整示例：
#
# server {
#     listen 443 ssl;
#     server_name daggerheart.cn;
#
#     location /SRD/api/ {
#         proxy_pass http://127.0.0.1:5000/api/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
#
#     location /SRD/ {
#         alias /var/www/SRD/public/;
#         index index.html;
#         try_files $uri $uri/ /SRD/index.html;
#     }
# }
#
# 修改后执行: sudo nginx -t && sudo systemctl reload nginx
